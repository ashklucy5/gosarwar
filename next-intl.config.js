// next-intl.config.js (at project root, same level as package.json)
/** @type {import('next-intl').NextIntlConfig} */
module.exports = {
  locales: ['en', 'zh'],
  defaultLocale: 'en',
  messagesDirectory: './messages'
};